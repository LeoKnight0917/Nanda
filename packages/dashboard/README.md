# Nanda Dashboard

Lightweight Vite + React + TypeScript + Tailwind dashboard.

Quick start:

```bash
cd packages/dashboard
pnpm install
pnpm dev
```

Set `VITE_API_BASE` to point at the index-service (default: `http://localhost:3000`).
